language: cpp
compiler:
  - gcc
  - clang
before_install:
  - cd nsg-library
  - git submodule update --init --recursive
  - sudo apt-get install libasound2-dev
# Change this to your needs
script:
  - ./cmake_linux.sh linuxbuild
  - cd ..
  - cd linuxbuild
  - make
  - make test
